{
  "name": "my-vercel-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp -r public/* dist/ 2>/dev/null || (echo '<html><h1>Hello Vercel!</h1><p>Build: '$(date)'</p></html>' > dist/index.html)",
    "dev": "echo 'Use local server'"
  }
}
